name: yaon
description: A simple library to parse either a string from either yaml or json.
homepage: https://github.com/islandlifetechnologies/yaon
version: 2.0.2

environment:
  sdk: ^3.12.0

dependencies:
  logging: ^1.3.0
  yaml: ^3.1.3

dev_dependencies:
  flutter_lints: ^6.0.0
  test: ^1.31.2
  yaml_writer: ^2.1.0
