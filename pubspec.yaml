name: yaon
description: A simple library to parse either a string from either yaml or json.
homepage: https://github.com/islandlifetech/yaon
version: 2.0.0

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  logging: ^1.3.0
  yaml: ^3.1.3

dev_dependencies:
  flutter_lints: ^6.0.0
  test: ^1.31.2
  yaml_writer: ^2.1.0
